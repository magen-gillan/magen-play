# ملاحظات التنفيذ

النموذج الأولي الحالي يبني تدفقات الواجهة وحالة المستخدم محليًا، ويستخدم بيانات تجريبية مرئية حتى يمكن اختبار تجربة التنقل والتخصيص من دون الادعاء بأنها مكتبة الجهاز الحقيقية. تشغيل الملفات المحلية الفعلي يحتاج إلى مسح MediaStore على أندرويد وربط خدمة تشغيل خلفية عبر Media3 أو وحدة Expo أصلية.

يدعم `expo-audio` تشغيل الصوت ومراقبة موضع التشغيل، لكنه ليس محررًا صوتيًا. لذلك فإن قص الملف وتصدير نسخة جديدة يحتاجان إلى طبقة أصلية تستخدم Media3 Transformer أو FFmpeg مبنيًا داخل التطبيق، مع اختبار الصيغ والترميزات وحقوق التخزين في Android 13+. أما كتابة الوسوم داخل MP3/FLAC/M4A فتحتاج إلى مكتبة وسوم أصلية أو مكتبة Kotlin متوافقة مع الصيغة، مع التعامل مع SAF وMediaStore بدل الكتابة المباشرة إلى مسار غير مصرح به.

ميزة A-B loop قابلة للتنفيذ على مستوى المشغل باستخدام `seekTo()` ومراقبة الموضع؛ وللدقة العالية يجب نقل الحلقة إلى طبقة تشغيل أصلية أو Listener موثوق مع مراعاة أن إعادة التوجيه المتكرر من JavaScript قد تتأثر بجدولة الخيط. اختيار الغلاف من الجهاز يمكن ربطه بـ `expo-document-picker` أو `expo-image-picker`، ثم حفظ نسخة محلية دائمة باستخدام `expo-file-system`.

المرحلة التالية المقترحة هي إنشاء Native Module لأندرويد لمسح MediaStore، وقراءة صورة الغلاف المضمنة و`folder.jpg`، وتشغيل Media3 في foreground service، ثم إضافة طبقة تحرير الوسوم والقص. يجب إبقاء واجهة `Track` و`ABLoop` ثابتة حتى لا تتغير واجهة المستخدم عند استبدال طبقة البيانات التجريبية بالطبقة الأصلية.
