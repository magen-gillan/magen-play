# تصميم magen-play

## الهدف

تطبيق مشغل موسيقى محلي لأندرويد بواجهة عربية أولًا، يركز على الوصول السريع بيد واحدة، عرض غلاف الأغنية بوضوح، والتحكم في المكتبة والمشغل والتحرير من دون حساب أو مزامنة سحابية.

## افتراضات التصميم

- الاتجاه الأساسي هو الوضع الرأسي بنسبة 9:16.
- الواجهة تتبع معايير iOS HIG في وضوح التسلسل البصري، المساحات الآمنة، أزرار اللمس الكبيرة، صفحات الإعدادات المنفصلة، والنوافذ السفلية للإجراءات السياقية.
- البيانات محلية على الجهاز، مع إبقاء بنية التخزين قابلة لاستبدالها بمسح MediaStore أصلي لاحقًا.
- العربية هي اللغة الافتراضية، مع دعم النصوص المختلطة واتجاه RTL.

## الشاشات

### 1. المكتبة الرئيسية

تحتوي على عنوان التطبيق، زر البحث، زر الإعدادات، وشريط اختيار نوع العرض: جميع الأغاني، الألبومات، المجلدات، الفنانون، الأنواع، وقوائم التشغيل. يظهر الخيار المختار أولًا حسب إعداد المستخدم. تعرض الأغاني في قائمة كثيفة مع صورة الغلاف، الاسم، الفنان، المدة، وزر إجراءات السياق.

### 2. المشغل المصغر

يثبت فوق شريط التبويب عند وجود أغنية قيد التشغيل. يعرض الغلاف المصغر، الاسم، الفنان، التشغيل/الإيقاف، والتقدم. النقر عليه يفتح شاشة المشغل الكامل.

### 3. شاشة المشغل الكامل

تعرض الغلاف في منتصف الشاشة، مع ثلاثة أنماط قابلة للاختيار من الإعدادات: مربع ثابت، دائرة ثابتة، ودائرة تدور أثناء التشغيل. أسفل الغلاف يظهر اسم الأغنية والفنان وشريط التقدم وأزرار السابق/التالي والتشغيل والتكرار والتبديل العشوائي. زر إضافي يفتح خيارات الأغنية مثل التعديل والقص وتكرار مقطع A-B وإضافة إلى قائمة تشغيل.

### 4. محرر بيانات الأغنية

نموذج محلي لتعديل الاسم، الفنان، ألبوم الفنان، الألبوم، السنة، النوع، رقم المسار، القرص، التعليق، والكلمات. يحتوي على معاينة الغلاف، زر اختيار صورة من الجهاز، زر إزالة الغلاف، وخيار حفظ التغييرات إلى الملف عندما تسمح صلاحيات وصيغة الملف بذلك. في النموذج الأولي تحفظ التغييرات كطبقة بيانات محلية واضحة إلى أن تتم إضافة كاتب وسوم أصلي.

### 5. قص الأغنية

شاشة تعرض اسم الملف وموجّهًا زمنيًا بيدين لتحديد البداية والنهاية، مع أزرار تشغيل الجزء المحدد، إعادة الضبط، وتصدير نسخة جديدة. يجب ألا يستبدل الملف الأصلي تلقائيًا؛ يكتب نسخة باسم آمن مثل `اسم-الأغنية-مقصوصة`.

### 6. تكرار A-B

شاشة أو لوحة سفلية لاختيار نقطة A ونقطة B بالدقائق والثواني، مع تشغيل فوري للمنطقة وإمكانية حفظها كعلامة باسم. تتضمن خيارات تكرار مستمر، عدد مرات محدد، وترك التكرار. هذه ميزة مناسبة للتدرب على مقطع موسيقي أو تعلم لغة.

### 7. الإعدادات

تنقسم إلى: بداية التطبيق، مظهر المشغل، سلوك التشغيل، المكتبة والمسح، الإشعارات، الصوت، النسخ الاحتياطي المحلي، واللغة. إعداد بداية التطبيق يتيح اختيار الأغاني أو الألبومات أو المجلدات أو الفنانين أو قوائم التشغيل.

### 8. قوائم التشغيل

تعرض القوائم المحلية، مع إنشاء قائمة جديدة، إعادة التسمية، ترتيب الأغاني، الحذف، والتثبيت في الصفحة الرئيسية.

## التدفقات الأساسية

1. يفتح المستخدم التطبيق، فيظهر نوع المكتبة الذي حدده في الإعدادات. يضغط على أغنية، فيظهر المشغل المصغر، ثم يضغط عليه لفتح المشغل الكامل.
2. من المشغل الكامل يضغط المستخدم على الغلاف أو زر العرض، فيبدّل بين المربع والدائرة والدائرة الدوارة، ويحفظ الاختيار فورًا.
3. من قائمة الأغنية يختار «تعديل البيانات»، يغير الغلاف والاسم والوسوم، ثم يحفظ. يظهر تنبيه يوضح هل حُفظ التغيير داخل الملف أم محليًا فقط.
4. من قائمة الأغنية يختار «قص»، يحدد نقطتي البداية والنهاية، يعاين الناتج، ثم يصدّر نسخة جديدة مع بقاء الأصل.
5. من المشغل يختار «تكرار A-B»، يحدد A وB، يبدأ التشغيل الحلقي، ويمكنه حفظ العلامة أو إيقافها.
6. من الإعدادات يختار المستخدم العرض الابتدائي للمكتبة، فيُستخدم عند فتح التطبيق التالي.

## الهوية البصرية

- اللون الأساسي: بنفسجي ليلي `#6D5DF5` ليعكس الصوت والإبداع.
- الخلفية الداكنة: `#0E0D14`.
- السطح: `#191724`.
- النص الأساسي: `#F7F5FF`.
- النص الثانوي: `#A9A4BD`.
- لون النجاح: `#5FD6A5`.
- لون التحذير: `#F4BE6A`.
- في الوضع الفاتح تستخدم خلفية `#F8F7FC` وسطح `#FFFFFF` مع نفس البنفسجي الأساسي.
- الصور مربعة بزوايا 18 نقطة في البطاقات، ودائرية فقط عندما يختار المستخدم نمط الدائرة.
- الحركات قصيرة وهادئة، من 120 إلى 280 مللي ثانية، ولا تعتمد على الحركة لإيصال معنى أساسي.

## بنية البيانات المقترحة

```ts
type Track = {
  id: string;
  uri: string;
  title: string;
  artist: string;
  album: string;
  albumArtist?: string;
  artworkUri?: string;
  durationMs: number;
  folder?: string;
  genre?: string;
  year?: number;
};

type PlayerSettings = {
  initialLibrary: "songs" | "albums" | "folders" | "artists" | "playlists";
  artworkMode: "square" | "circle" | "spinning";
  theme: "system" | "light" | "dark" | "amoled";
};

type ABLoop = {
  trackId: string;
  startMs: number;
  endMs: number;
  repeatCount: number | "infinite";
  label?: string;
};
```

## المزايا الإضافية المفيدة

يشمل التصميم أيضًا البحث السريع، المفضلة، سجل الاستماع، آخر ما أضيف، مؤقت النوم، سرعة التشغيل، الفجوات السلسة، التحكم من الإشعار وشاشة القفل، دعم Android Auto، استيراد وتصدير قوائم التشغيل، استبعاد مجلدات، ReplayGain، كلمات LRC، معادل صوتي، وإحصاءات تشغيل محلية. تُنفذ المزايا التي تحتاج صلاحيات أو كودًا أصليًا في مراحل منفصلة بدل تقديم واجهات وهمية.
